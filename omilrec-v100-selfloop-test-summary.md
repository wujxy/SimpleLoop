# OMILREC v1.0.0 SimpleLoop 自主优化 —— 12 轮汇总（前 4 轮原始 + 续跑 8 轮）

- **Run**: `omilrec-v100-selfloop-test`（run-dir: `SimpleLoop/runs/omilrec-v100-selfloop-test/`）
- **Baseline**: `8218ba3` (v1.0.0 原始未优化代码)，**924.53 ms/evt**
- **Correctness gate**: e2e bit-identical，10 events 输出 vs `reference/ref_10evt.root`（tolerance 0，15 个 RecVertex float 字段 + 1 个 int）
- **所有 12 轮 correctness gate 均 PASS**
- 表格中"(…)"为该角色单次 claude 调用耗时；speed 行末括号为 vs baseline 累计降幅
- R1–R4 来自原始 4 轮 run；R5–R12 来自 `--continue` 续跑（baseline eval 重跑一次，链从 R4 接）

## 汇报版简表

| 轮 | proposal（方向） | executor 执行 | judger 评判 | speed (ms/evt) |
|----|------------------|---------------|-------------|----------------|
| **R1** | PMT 不变量 SoA 化 + QPDF cache：把几何、PMT 参数、hit 输入、QPDF 插值结果移出 FCN 内层循环，改为事件/相位级缓存。 | 成功提交 `ef563ec`，改 `OMILRECV2.cc/.h`；建立 geometry/para/hit/QPDF cache，内层 getter 和重复 QPDF 插值改为数组读。 | score **0.83**，risk **low**；correctness PASS 10/10；认为是 clean bit-faithful hoist，主要风险是后续维护时 cache refresh 不能漏。 | **744.06**（vs baseline **-19.5%**） |
| **R2** | NPE-map 插值 per-FCN 预计算：每次 FCN call 先算 1440 个 theta bin，PMT 循环内只读表并做原有 blend。 | 成功提交 `9f3173d`，改 `OMILRECV2.cc/.h`；新增 NPE 预计算表，覆盖 Use3DMap / mixing / 1D map 三条路径。 | score **0.78**，risk **low**；correctness PASS 10/10；确认是低风险 pure hoist，但收益空间已基本吃满。 | **671.82**（vs prior **-9.7%**，vs baseline **-27.3%**） |
| **R3** | time-only 阶段 PMT 裁剪：`enableQInfo=false && enableTimeInfo=true` 时只遍历 time-eligible hit PMT，跳过无似然贡献的非 hit PMT。 | 成功提交 `b14461f`，改 `OMILRECV2.cc/.h`；每事件构建升序 `m_time_hit_pmt`，time-only FCN 走 hit-PMT 列表，其它阶段仍走全 PMT。 | score **0.88**，risk **medium**；correctness PASS 10/10；当前最佳，主要风险是依赖 `m_NPE/m_UfrmScale` 在 time-only 后不被消费的隐式不变量。 | **485.18**（vs prior **-27.8%**，vs baseline **-47.5%**） |
| **R4** | charge-only 跳过 time 几何 + 部分 time-PDF hoist：charge-only 相跳过无用 time 几何，`enableRDTimePdf=false` 时预计算 RecNpe==2 相关 time-PDF 标量。 | 成功提交 `60ff65d`，改 `OMILRECV2.cc`；增加 `enableTimeInfo` 守卫和 FCN-invariant time-PDF 预计算。 | score **0.30**，risk **low**；correctness PASS 10/10；但性能较 R3 回退，判断新增分支/icache 成本大于节省。 | **515.02**（vs prior **+6.1%** 回退，vs baseline **-44.3%**） |
| **R5** | enableTimeInfo 拆双循环：把 PMT 循环按 `enableTimeInfo` 拆成两个 FCN 级分支（charge-only 走精简循环体），修 R4 的 per-PMT 分支开销；附带 hoist `abs(m_R)` / `EvtR`。 | 成功提交 `b981de3`（1 file: `OMILRECV2.cc`）；FCN 级 `if(enableTimeInfo)` 选两个循环体之一，charge-only 体省去死 time 算术。 | score **0.25**，risk **low**；correctness PASS 10/10；**+5.0% 回退**。judger 判 enableTimeInfo-split 方向 0/2（连退两次），建议 revert 到 R3 best 攻别的路径。 | **540.59**（vs prior **+5.0%** 回退，vs baseline **-41.5%**） |
| **R6** | NPE-map bin-search 全 hoist：一次 FCN call 内 `(RVar,m_Theta)` 对全 1440 直方图不变，只有直方图指针变；把 bin-search 算一次，对 1440 个直方图批量取 4 值 + 同序 blend。 | 成功提交 `0fb1bc5`（3 files: `RecHelper.cc/.h` + `OMILRECV2.cc`）；RecHelper 加批量 filler `FillNPEMapTable_*`，FCN precompute 改一次批量调用填全表。 | score **0.87**，risk **low**；correctness PASS 10/10；**-13.1% 真涨**。bin-search 杠杆吃满（1440→1/FCN call），建议 profile 找新主导相。 | **469.88**（vs prior **-13.1%**，vs baseline **-49.2%**） |
| **R7** | NPE-once skip + 不变量 elision：跳过 NPE-map precompute 在 `npe_interp` 已填时的重复，elide 几个小不变量。 | 成功提交 `857972d`（2 files）；加 NPE-once 守卫 + 几处不变量省略。 | score **0.60**，risk **low**；correctness PASS 10/10；**-2.2% 小赚**。judger 指出 geometry+para SoA 仍是未实现的杠杆，建议 profile。 | **459.32**（vs prior **-2.2%**，vs baseline **-50.3%**） |
| **R8** | ALL-invariant SoA（重提 R0 杠杆）：又提把 geometry/para/hits 全 hoist，称"round 0 validated 但从没建"。 | **空提交，executor 未改任何代码**（empty diff）。 | score **0.05**，risk **low**；无代码变更，speed 沿用 prior 459.32（PASS）。judger：下轮真正建 SoA，先切片落地。 | **(空)** 沿用 459.32 |
| **R9** | GEOMETRY SoA 切片：只做 `m_pmtx/pmty/pmtz/pmtr` + standalone-CalLTOF `EvtR` hoist（para/hits 留给后续 stacked commit）。 | 成功提交 `5f90c2d`（2 files）；`initialize()` 建 geometry SoA，FCN/CalLTOF 换数组读，hoist `EvtR`。 | score **0.35**，risk **low**；correctness PASS 10/10；**+12.1% 回退**。judger：只 hoist 了 CalLTOF 的 EvtR，FCN 主体 11 个 virtual call 没动，无真实加速。 | **514.72**（vs prior **+12.1%** 回退，vs baseline **-44.3%**） |
| **R10** | ALL-invariant SoA（又重提）：一气 hoist geometry+para+hits 全 SoA，称 R9 回退只因 partial slice，全做才有效。 | 成功提交 `0c40b93`（1 file）；落 para/hits SoA + `vector<bool>→char` 微调。 | score **0.42**，risk **low**；correctness PASS 10/10；**+0.3% 噪声**。judger：SoA 已落地，别再微调 `m_isdyn`，攻主导成本。 | **516.06**（vs prior **+0.3%** 噪声，vs baseline **-44.2%**） |
| **R11** | time-PDF histogram 指针解析 hoist：`(TIdA,TIdB,flag,kk)` 对全 PMT FCN-invariant，把 24 个 `m_omilrecSvc->get*Pdf` 虚 dispatch 解析移到 per-FCN-call precompute，FCN 内只读指针。 | 成功提交 `9dfdbcc`（1 file: `OMILRECV2.cc`）；FCN 前 precompute 所有 time-PDF 直方图指针，per-PMT 块读指针变量。 | score **0.80**，risk **low**；correctness PASS 10/10；**-11.0% 真涨**。pointer-resolution 是正确的 `enableRDTimePdf`-aware 杠杆（修正了 R3/R4 误判 `enableRDTimePdf=false` 的 grounding）。 | **459.50**（vs prior **-11.0%**，vs baseline **-50.3%**） |
| **R12** | time-PDF 插值轴元数据 + bin-search memoize：RecHelper 构造函数预计算 time-PDF 轴常量（`dx/cxf/cxl/...`），加 last-bin cache 缓存 `(VVar0,tres)` 的 bin 解析。 | 成功提交 `55254ac`（2 files: `RecHelper.cc/.h`，不动 `OMILRECV2.cc`）；ctor 预计算轴元数据 + 单 entry last-bin cache，hit 时跳 bin-search 只做 4 数组读 + blend。 | score **0.20**，risk **low**；correctness PASS 10/10；**+13.2% 回退**。judger：last-bin cache 的成员读 + 不可预测分支开销 > ~25-flop bin-search 节省，"同 (VVar0,tres) 命中率"假设被实测证伪。建议剥掉 cache 只留 ctor 轴 hoist 再测，若仍慢则该路径已耗尽，**先 profile**。 | **519.94**（vs prior **+13.2%** 回退，vs baseline **-43.7%**） |

| 其它数字 | R1 | R2 | R3 | R4 | R5 | R6 | R7 | R8 | R9 | R10 | R11 | R12 |
|----|----|----|----|----|----|----|----|----|----|----|----|----|
| proposer 耗时 | 403s | 332s | 2309s | 1134s | 1363s | 1967s | 3414s | 970s | 775s | 204s | 351s | 1309s |
| executor 耗时 | 2012s | 255s | 540s | 560s | 1409s | 1700s | 710s | 951s | 354s | 501s | 567s | 1330s |
| judger 耗时 | 150s | 53s | 49s | 53s | 135s | 227s | 305s | 67s | 65s | 185s | 166s | 199s |
| 总轮长 | ~44min | ~11min | ~48min | ~29min | ~49min | ~65min | ~74min | ~33min | ~20min | ~15min | ~18min | ~47min |

R3 proposer 2309s 异常长——经查 transcript，64 个 Bash 调用**全部只读**（grep/sed/wc/find/git log/ls），0 个 Edit/Write/Build，是在源仓库 + junosw 树里读代码做提案调研，未越界（见 `simpleloop-proposer-no-hard-readonly` memory）。R7 proposer 3414s 为全程最长，同样为代码调研。

## 详细轮次记录

| 轮 | proposal（方向） | executor 执行 | judger 评判 | speed (ms/evt) |
|----|------------------|---------------|-------------|----------------|
| **R1** | 把所有 PMT-indexed 不变量从 `Calculate_EVLikelihood` 内层 PMT 循环 hoist 出来，进 OMILRECV2 持有的 flat SoA 数组，只在相界刷新。四组 cache：(1) GEOMETRY `pmtx/pmty/pmtz/pmtr` 在 `initialize()` 一次建好，干掉每 PMT 的 `ALL_LPMT_pos[i].X()/.Y()/.Z()` 解引用 + `sqrt`；(2) PARA SCALARS `isdyn/lpde/ldnr/lq1/lqres/lgain/timeoffset` 在 `LoadPMTPara` 后快照，替换内层 ~10 次 virtual `IRecParaTool` getter 调用（`GetLGain` 原被调两次）；(3) INPUT HITS `charge/time/npe` 在每个会改写 hit 的相界 `RefreshHitCache()`；(4) 同机制扩展：QPDF `proTemp[k]` 只依赖 `(PMT_Hit, isDyn, Q1, S1)` 全相不变，每相建一次，FCN 只留 Poisson 递推 + 累加。全部保留算术与求值顺序以过 bit-identical gate。 | 执行成功，commit `ef563ec`（2 files: `OMILRECV2.cc` +211/−42、`OMILRECV2.h` +38）。在 `initialize()` 建 geometry+para cache、`execute()` 各相界调 `RefreshHitCache`/`RefreshQpdfCache`、FCN 内层 getter 全换数组读。judger 复核：bit-faithful 验证通过（geometry 复算同一 `sqrt`、para/hit 是 getter 返回值逐字拷贝、QPDF cache 用同一 `InterpolateQPDFTH1D`/`Gaus`/`probPrcs`/early-break、Poisson 递推顺序不变；`PedThres` 守卫在 cache builder 与 FCN 两处都保留；`CalLTOF` 也换缓存值同值）。相界刷新点覆盖完整。 | score **0.83** risk **low**。"Solid SoA hoist + QPDF cache，clean bit-faithful refactor"。实测 744.06 vs prior 924.53 (**−19.5%**)，bit-identical 10/10。打在 0.70–0.90 带：真实 −19.5% 无回退、bit-identical 保留、代码干净、非平凡重构（三 cache + 同机制扩展）；不到 0.90 因相对 5–10× 目标增益有限且 QPDF cache 引入一个后续轮次要维护的不变量。**风险**：低。唯一潜在隐患是维护性——QPDF cache 正确性依赖 `proTemp` 今后仍只是 `(PMT_Hit,isDyn,Q1,S1)` 的函数、且每个新加 `Set_LHIT_Charge/Time` 调用点后都要 refresh，否则静默 stale。方向建议：QPDF 插值已 O(1)/FCN，剩余热点是 per-PMT `exp(-m_expected_PE)` + Poisson 递推和时间似然分支，考虑 hoist LTOF 无关项或缩 PMT 迭代到 hit-only。 | **744.06** (−19.5% vs baseline) |
| **R2** | 把 NPE-map 插值从 PMT 循环 hoist 出来，改成 per-FCN-call 在 1440 theta bins 上预算。利用查询点 `(RVar, m_Theta)` 在一次 FCN call 内对所有 PMT 不变、只有 per-PMT bin index `GrMapIdA = theta/MapThetaStep` 变。原先 17613 PMT 每个每次 FCN call 都调 `InterpolateNPEMapTH2F/TH1D`（双线性/1D 分段线性，~20–40 flops + 4 数组读 + 若干分支），Minuit O(几百)次/event → ~3.5M NPE 插值/event，是 round 0 后剩余的主要 LTOF 无关成本。机制：PMT 循环前（`RVar/m_Theta` 已知，~line 1148）一次性预算 length-1440 表 `P[j]`，循环内把每 PMT 两次插值换成表读，blend 算术 `(1-W)*P[A]+W*P[B]` 与边界回退 `P[ThetaPMTNum-1]` 逐字节保留；`isMixingPhase` 分支（`m_average_PE = interp(LMu2D[0],…)` 完全 PMT 无关）hoist 成单标量，砍掉 17613 中的 17612 次插值。表存 `double m_npe_interp[ThetaPMTNum]`（编译期常量 1440，无分配风险）。 | 执行成功，commit `9f3173d`（2 files: `OMILRECV2.cc` +37/−9、`OMILRECV2.h` +12）。precompute 分支（Use3DMap / else isMixingPhase / else 1D）与循环分支结构一一对应，每个循环读命中已填 slot，无未初始化读路径；对象本就有 per-call member state，无新线程隐患。judger 复核：pure hoist——同函数、同参数、同求值顺序与类型、同边界分支序，唯一行为差异是 1440 bins 每次 FCN call 都全填（即便某些 bin 永不被 non-lost PMT 访问，额外功而非正确性问题，实测仍净赚）。 | score **0.78** risk **low**。"NPE-interp hoist: 671.82 ms/evt, −9.7% vs prior, −27.3% vs baseline, bit-identical gate PASS。Clean bit-faithful table precompute"。实测 671.82 vs prior 744.06 (**−9.7%**)，vs baseline **−27.3%**。0.78：方向对、低风险、gate 过；不到更高因仍 ~1.38× vs baseline 对 5–10× 目标、且该 hoist 的空间已基本吃满。**风险**：低。潜在隐患：precompute 无条件跑即便 non-lost PMT 很少，可能在极少 hit 的边缘事件回退——但实测事件净赚且 gate 过。方向建议：NPE 插值已非主导，profile 找下一热点（大概率 per-PMT Poisson/log/exp 尾或 QPDF path）再提下一 hoist。 | **671.82** (−27.3% vs baseline) |
| **R3** | 把 time-only 最小化阶段（`enableQInfo=false && enableTimeInfo=true`，5 参数 Minuit fit，execute 里最 FCN-call-heavy 的最小化）的 PMT 迭代从全 17613 缩到仅 hit PMTs。依据：该阶段非 hit PMT（`RecNpe=int(LHIT_NPE[i))` 不在 `[1, UsedTnPENum=6]` 或 z-filter 不过）的 `QInfoIsValid=false` 且 `timeInfoIsValid=false`，对 `m_Likelihood` 贡献恰为零——Q 块/Q 累加器受 `QInfoIsValid` 守卫、`CalLTOF`/time 块/T 累加器受 `timeInfoIsValid` 守卫。唯二的 per-PMT 写 `m_NPE += …`/`m_UfrmScale += …` 在 time-only 最小化与紧接其后的 charge+time 最小化之间不被消费（中间只赋 `rec_TR/Theta/Phi` 等、`RemoveDN`/`CoorTransform`/重置 recminuit，不读这俩；`m_NQE` 只在 charge-bearing 最小化后于 472/702/884 重算），故跳过非 hit PMT 既不改 Minuit 看到的似然（→ 同轨迹同结果）也不改任何 RecVertex 字段。机制：`OMILRECV2.h` 加 `std::vector<int> m_time_hit_pmt`，`execute()` 里 `setBadPMTRUNID`/`LoadPMTInput` 后每事件建一次（`IsLost` run-fixed、`LHIT_NPE` event-fixed 且 `RemoveDN` 不改它、z-filter 只依赖常量 `pos_z` 与 mode → 集合 event 内不变），升序 push 保累加顺序；FCN 里 `(enableTimeInfo && !enableQInfo)` 走 `m_time_hit_pmt`，其余相走全 0..TotalLPMT-1。 | 执行成功，commit `b14461f`（2 files: `OMILRECV2.cc` +54、`OMILRECV2.h` +13）。`BuildTimeHitPMT` 在 execute 入口建一次，精确编码 FCN 的 `timeInfoIsValid` 条件（`!IsLost`、`int(LHIT_NPE)`∈`[1,UsedTnPENum]`、z-filter 含 `UseAll`/`UseAboveZ` 分支与 `int()` 截断）。judger 复核：升序 push 保累加顺序，FP 求和 bit-faithful；time-only 阶段缩到几百 PMT。已独立复核（另开 worktree checkout `b14461f` → 独立 cmake build → 独立跑 `test_consistency.py`）得 `OK: All 10 events match (bit-identical)`，与记录一致。 | score **0.88** risk **medium**。"Real measured win: 485.18 ms/evt vs prior 671.82 (**−27.8%**) and baseline 924.53 (**−47.5%**), e2e bit-identical gate PASS (10/10)"。0.88：方向对、metrics 可见的真改进、干净有注释的重构、无 gate 回退。**风险 medium（非 low）两个具体理由**：(1) bit-identical 论证依赖一个 eval 不直接测的不变量——time-only 阶段跳过的 PMT 仍在写的 `m_NPE`/`m_UfrmScale` 在该阶段与下次覆盖它们的 charge-bearing 最小化之间**不被消费**。proposer 称 628–657 只赋 `rec_TR/Theta/Phi` 等、不读 `m_NPE/m_UfrmScale`，`m_NQE` 只在 charge-bearing 后于 472/702/884 重算——合理但脆弱：未来改最小化顺序或某 steering 跳过 charge-bearing 最小化会让跳过的写静默可观测。建议加 debug cross-check 或注释钉死覆盖点。(2) z-filter 与 `RecNpe=int(m_hit_npe[i])` 编码必须与 FCN 的 `timeInfoIsValid` 完全一致，10-event 样本只验证了当前输入。方向建议：time-only 相已吃尽，下一热点转到 charge+time 相（两 flag true、全 17613 sweep）——非 hit PMT 在该相贡献非零 charge 似然故不能直接 skip，转而做 bit-identical 结构优化（hoist per-PMT 常量 geometry/expected_PE/dmu，或预算/SOA hit-PMT 数据布局）；并考虑先硬化本轮不变量（debug build 强制全循环 diff `m_NPE/m_UfrmScale`）再依赖。 | **485.18** (−47.5% vs baseline) |
| **R4** | 两部分 bit-identical hoist。**Part A**（大杠杆，round-2 反馈"attack charge+time 相"的直接延续但留下 charge-only 相未动）：charge-only 相（`enableTimeInfo=false`，phase1 QMLE 458-460、phase5 final-energy 765-766、两直调 708/732→735/740）里 `timeInfoIsValid` 对每 PMT 恒 false，故 `LTOF_temp` 不用、`tres` 块不进、T 累加器不执行——但今天 17613 PMT 仍无条件算 `dstn=sqrt`(1164-1166)、`VVar0`(1168-1169)（`CalLTOF` 受守卫但 dstn/VVar0/tres 不受）。用 `if(enableTimeInfo)` 守卫 dstn/VVar0/tres/LTOF_temp，charge-only 相整段省去。**Part B**：charge+time 相（phase3 两 flag true，round-2 明指的全 17613 sweep）里 `InterpolateTimePDFTH1F` 的 Ia/Ib/Ic 调用只依赖 `(TIdA/TIdB, flag, VVar0)`，不依赖 `tres`/`RecNpe`；默认 `enableRDTimePdf=off`(line 126) 时 `VVar0=19.43` 对全 PMT 常量，故 6 调用/PMT（RecNpe==2 子分支）FCN 内全 PMT 相同 → hoist 成单 6-scalar precompute。`enableRDTimePdf` 开时 `VVar0=dstn` 随 PMT 变，不 hoist。 | 执行成功，commit `60ff65d`（1 file: `OMILRECV2.cc` +70/−19）。Part A 加 `enableTimeInfo` 守卫 dstn/VVar0/tres；Part B 在 `(enableTimeInfo && !enableRDTimePdf)` 时 precompute 12 个 FCN-invariant `InterpolateTimePDFTH1F`（Ia/Ib/Ic @ TIdA/TIdB, dyn+mcp）一次/FCN call，RecNpe==2 子分支读标量。judger 复核：refactor 干净，bit-identical 论证成立（hoisted 标量用同函数同参数 `VVar0=19.43` 常量；跳过的 dstn/tres 只在 `timeInfoIsValid` 守卫块内被读），e2e gate 确认 CORRECTNESS=PASS 10/10。 | score **0.30** risk **low**。"**REGRESSION** vs prior: 515.02 ms/evt vs 485.18 (**+6.1%**); correctness PASS, still **−44.3%** vs baseline"。实测 515.02 vs prior 485.18 **+6.1%**（回退），仍比 baseline 快 44.3%。judger 推断原因：Part A 加的 `if(enableTimeInfo)` 分支跑在 hot 的 charge+time phase-3 循环（17613 PMT × 多 FCN call，该相 enableTimeInfo=true）里啥也没省却加了 per-PMT 分支；而它省 sqrt 的 charge-only 相 FCN-call 密度没假设的高，或上轮已攻过主导成本。Part B 的 hoist 只在 phase3 RecNpe==2 子分支触发（可能很少走），12 调用 precompute 省得少，而加的 `if/else` 分支 + 24-scalar precompute 块增加 icache/分支预测压力。净：开销 > 节省。0.30：方向错（实测回退）但 gate 过——bit-faithful 且干净但本轮不该得分。**风险 low**：无算术改变、同算子/求值序/类型、gate 过、hoist key 真 FCN-invariant、Part A 守卫可证死路径。方向建议：revert 本轮 diff，攻一个在**所有相**都跑的热路径（不只 charge-only 或 RecNpe==2）：候选 Q-似然/QInfo 块、`CalLTOF` 距离、`Use3DMap` theta/GrMapId 查找（每相每 PMT 无条件执行）。**先 profile 确认主导 per-PMT 成本再 hoist。** | **515.02** (−44.3% vs baseline) |
| **R5** | enableTimeInfo 拆双循环（**显式定位为"修 R4 而非重复"**）：R4 Part A 把 `if(enableTimeInfo)` 守卫放在循环体内，因 `enableTimeInfo` 是 OMILRECV2 成员、编译器无法证其 loop-invariant（循环调成员函数可能改 `this`），无法 hoist/unswitch → 退成每 PMT 重检的分支，开销 > 节省才 +6.1% 回退。修法：把分裂提到 FCN 级——循环前 `const bool doTime=enableTimeInfo;` 选两个循环体之一，整 FCN call 仅一分支（完美预测，enableTimeInfo 相内常量），零 per-PMT 分支。charge-only 体只留 IsLost skip/isDyn/pos+PMTR/cos_theta+theta+acos/NPE 表读+blend/m_expected_PE/dmu/fdmu/Q1·S1/PMT_Hit·RecNpe/Q 块(含 QPDF cache)/Q 累加/m_NPE·m_UfrmScale；full 体不变。附带 free hoist：`abs(m_R)` per-FCN、charge+time 体里 inline CalLTOF 用已 hoist 的 geometry SoA + per-FCN `EvtR`。 | 执行成功，commit `b981de3`（1 file: `OMILRECV2.cc`）。FCN 级 `if(enableTimeInfo)` 选 charge-only 精简体或 full 体；charge-only 体省去 dstn/VVar0/tres/LTOF_temp/时间块/T 累加器全部死算术；附 `abs(m_R)` 与 inline CalLTOF `EvtR` hoist。judger 复核：dead-path 论证成立（enableTimeInfo=false 时时间路径量确为死，charge-only 体只省死量），bit-identical PASS。 | score **0.25** risk **low**。"**540.58 ms/evt, +5.0% vs prior — regression**; correctness PASS, risk low。**enableTimeInfo-split 方向 0/2（连退两次）**。revert 到 round-2 best 攻别的热路径：Q-block Poisson 递推或 NPE-map 插值"。实测 540.59 vs prior 515.02 **+5.0%**（回退），vs baseline −41.5%。judger 推断：charge-only 相 FCN-call 密度低于假设，或 R3 已攻过主导成本；双循环体增加代码体积/icache 压力；inline CalLTOF 的 EvtR hoist 在 full 体的收益不足以抵消 charge-only 体的结构开销。**关键**：这是 enableTimeInfo-split 方向**第二次失败**（R4/R5 均回退），judger 明确判该方向 0/2，建议放弃。 | **540.59** (−41.5% vs baseline) |
| **R6** | NPE-map **共享 bin-search** 全 hoist（R5 judger 点名的"NPE-map 插值"杠杆）：一次 FCN call 内 `(RVar, m_Theta)` 对全 1440 theta-bin 直方图 `LMu2D[j]/LMu1D[j]` 不变（且对全 17613 PMT 不变），只有直方图指针变。bin-search（`interp_TH2F_piecewiseCenterXY_arrayF` 的 i/tx/j/ty，RecHelper.cc:108-152；`interp_TH1D_piecewiseCenter_arrayD` 的 i/t，194-228）只依赖 `(RVar, m_Theta)` + 共享 binning，**不依赖 j**。今天同一 bin-search 重跑 1440 次/FCN call（R2 的 P[j] precompute）或 17613 次（baseline per-PMT），每次 ~10-20 flops + 4-5 分支 + 函数调用，而只有 4 数组读 + blend 随直方图变。机制：RecHelper 加批量 filler `FillNPEMapTable_TH2F(const TH2F* hists[], int n, double rvar, double theta, double out[])` 及 TH1D 变体——**bin 索引 (i,j,tx,ty) 算一次**，对 n=1440 直方图各取 4 值做**同序** blend `(Q11*(1-tx)+Q21*tx)*(1-ty)+(Q12*(1-tx)+Q22*tx)*ty`。FCN precompute 的 1440 次 `InterpolateNPEMap*` 换成 1 次批量调用。per-PMT 下游 blend 算术逐字节不变。 | 执行成功，commit `0fb1bc5`（3 files: `RecHelper.cc`/`RecHelper.h`/`OMILRECV2.cc`）。RecHelper 加批量 filler，FCN precompute 改一次批量填全 1440 表；judger 复核：每个 P[j] 是同序同 4 值的同 blend，bin 索引一致，m_average_PE 及下游全不变，bit-identical PASS。 | score **0.87** risk **low**。"**PASS, 469.88 ms/evt vs prior 540.59 (-13.1%), gate 10/10 bit-identical**。NPE-map bin-search lever now fully hoisted (1440→1/FCN call)"。实测 469.88 vs prior 540.59 **−13.1%**（真涨），vs baseline **−49.2%**。0.87：方向对、真涨、clean、无回退；不到更高因仍 ~1.97× vs baseline 对 5–10× 目标。**风险**：低，pure hoist（同函数同参数同求值序同边界序）。方向建议：bin-search 杠杆吃满，**profile FCN 找新主导相**（per-PMT QPDF/time-PDF 插值或 Poisson/exp）再攻。 | **469.88** (−49.2% vs baseline) |
| **R7** | NPE-once skip + 小不变量 elision：NPE-map precompute 在 `m_npe_interp` 已填时跳过重复填充；elide 几个 FCN-invariant 小量。属过渡轮，把 R6 的 precompute 收尾。 | 成功提交 `857972d`（2 files）；加 NPE-once 守卫 + 几处不变量省略。judger 复核：bit-identical PASS，低风险 elision。 | score **0.60** risk **low**。"**NPE-once skip: 459.3 ms/evt (-2.2% vs prior 469.9, -47.8% vs base)**; bit-identical PASS 10/10。Low-risk invariant elision"。实测 459.32 vs prior 469.88 **−2.2%**（小赚），vs baseline **−50.3%**（首次破 50%）。0.60：方向对低风险但收益小。方向建议：geometry+para SoA（per-FCN virtual dispatch）仍是未实现杠杆，**profile FCN 找主导相**。 | **459.32** (−50.3% vs baseline) |
| **R8** | ALL-invariant SoA（**重提 R0 杠杆**，称"round 0 best-scoring idea，grounded against actual current baseline source, verified pristine — none of prior artifacts exist yet"）：又提把 geometry/para/hits 全 hoist 进 SoA，~10 virtual call × 17613 PMT × O(几百) FCN call × 每相。 | **空提交**：executor 未改任何代码（empty diff），judger 记 `no code change this round`。 | score **0.05** risk **low**。"No code change this round (empty diff); SPEED stays at prior 459.32 (PASS), no measured gain to credit"。无代码无加速，沿用 459.32。judger：下轮**真正建 validated geometry+para+hits SoA hoist——切片落地**：先 land geometry SoA（`m_pmtx/pmty/pmtz/pmtr` in initialize + CalLTOF + hoist EvtR）作 self-contained 首提交，再 stack para 和 hits。 | **(空)** 沿用 459.32 |
| **R9** | GEOMETRY SoA **切片**（执行 R8 judger 的"先 land geometry SoA"指令）：只做 `m_pmtx/pmty/pmtz/pmtr` 在 `initialize()` 一次建好 + FCN 与 CalLTOF 换数组读 + hoist standalone-CalLTOF `EvtR`（FCN 体 `EvtR` 上轮已 hoist）。para/hits SoA 留给后续 stacked commit。 | 成功提交 `5f90c2d`（2 files: `OMILRECV2.cc/.h`）。`initialize()` 建 geometry SoA，FCN/CalLTOF 读 `m_pmtx[i]` 等，hoist CalLTOF `EvtR`。judger 复核：bit-identical PASS（SoA 同值同 sqrt，EvtR 同值）。 | score **0.35** risk **low**。"Bit-identical PASS, but **+12.1% vs prior (459.3→514.7) — regression on 10-evt noise**。Change is only standalone-CalLTOF EvtR hoist (FCN body already hoisted last round), so no real speedup this round"。实测 514.72 vs prior 459.32 **+12.1%**（回退），vs baseline −44.3%。judger：只 hoist 了 CalLTOF 的 EvtR（FCN 体的 geometry 上轮已做），FCN 主体 11 个 virtual call + 3 TVector3 deref 没动，故无真实加速；+12.1% 偏大但 10-evt 噪声亦有可能。方向建议：re-bench with repeats 确认噪声；然后 land para+hits SoA 全量。 | **514.72** (−44.3% vs baseline) |
| **R10** | ALL-invariant SoA **全量**（又重提，称 R9 回退"ONLY because partial slice"，修法是 FULL SoA 一气全做）：geometry+para+hits 全 SoA，`vector<bool>→char` 微调，~11 virtual call/PMT 全干掉。 | 成功提交 `0c40b93`（1 file: `OMILRECV2.cc`）。落 para+hits SoA + `m_isdyn` 改 `vector<char>`。judger 复核：bit-identical PASS，inv_dx 存但未替 live `/dx`（躲开 reciprocal 陷阱）。 | score **0.42** risk **low**。"PASS bit-identical; **516.06 ms/evt, +0.3% vs prior — noise, no real gain**。vector<bool>→char is sound but too small to measure"。实测 516.06 vs prior 514.72 **+0.3%**（噪声平），vs baseline −44.2%。judger：SoA 已落地，**别再微调 m_isdyn**，攻主导剩余 FCN 成本（NPE/QPDF/time-PDF 插值，或 Poisson/log/exp）。 | **516.06** (−44.2% vs baseline) |
| **R11** | time-PDF **histogram 指针解析** hoist（R10 judger 点名的"per-hit-PMT InterpolateTimePDFTH2F bin-search+interp 现主导"）：**关键 grounding 修正**——`conftest.py:158` 传 `--enableRDTimePdf`，故 gated/benchmark run 里 `enableRDTimePdf=true`（非源码默认 false）；R3/R4/R5 假设 `VVar0=19.43` 全错，正是其回退根因。修正后：(a) R3 bin-search 真活，`TIdA/TIdB/TIdW` 是真 FCN-invariant 标量；(b) `VVar0=dstn` 真随 PMT 变；(c) `flag` FCN-invariant。正确杠杆是 hoist **直方图指针解析**（非插值）：块内 24 个 `m_omilrecSvc->get*Pdf(TIdA|TIdB, 0|kk, flag)` 虚 dispatch 只取 `{TIdA,TIdB,flag,kk}`（全 FCN-invariant，不含 i/VVar0/tres），返回的 `TH2F*/TH1F*` 对全 PMT 相同，却每 hit-PMT 重解析。机制：FCN 前（TIdA/TIdB/flag 定后、PMI 循环前，仅 `enableTimeInfo && enableRDTimePdf`）把 24 指针 + RecNpe>2 的 `kk` 数组一次解析进 FCN-local 变量，per-PMT 块读指针变量，`InterpolateTimePDF*(ptr, VVar0, tres)` 调用与下游算术逐字节不变。 | 成功提交 `9dfdbcc`（1 file: `OMILRECV2.cc`）。FCN 前 precompute 全部 time-PDF 直方图指针（含 RecNpe>2 的 kk 数组），per-PMT 块读指针变量。judger 复核：每个传给 InterpolateTimePDF 的指针是同一对象（同 get* 同 FCN-invariant 参），同 (VVar0,tres)，返回同 double，pronpe/Pro_Ti/m_Likelihood 全 bit-identical，gate PASS。 | score **0.80** risk **low**。"**PASS bit-identical, 459.5 ms/evt vs 516.06 prior (-11.0%), -47.8% vs baseline**。Pointer-resolution hoist is the correct enableRDTimePdf-aware lever"。实测 459.50 vs prior 516.06 **−11.0%**（真涨），vs baseline **−50.3%**。0.80：方向对、真涨、修正了 R3/R4/R5 的 grounding 错误、clean。方向建议：per-hit-PMT `InterpolateTimePDFTH2F` bin-search+interp 现是主导剩余成本，binning FCN-invariant 但 tres per-PMT，考虑 hoist 轴/bin 元数据或缓存 last-bin index。 | **459.50** (−50.3% vs baseline) |
| **R12** | time-PDF 插值 **轴元数据 + bin-search memoize**（R11 judger 点名"hoist 轴/bin 元数据或缓存 last-bin"）：RecHelper 构造函数预计算 time-PDF 轴常量（`dx,inv_dx,cxf,cxl,dy,inv_dy,cyf,cyl`，从 200/400/0/40/0/600 派生，全常量却每次重算 2 除 + ~8 flops）；加单 entry **last-bin cache** 缓存 `(VVar0,tres)` 的 bin 解析 `(kx,tx,ky,ty,inrange)`，hit 时跳 ~25-flop + 2 除 bin-search 只做 4 数组读 + 同序 blend。只改 `RecHelper.{cc,h}`，不动 `OMILRECV2.cc`（保 R11 指针 hoist）。 | 成功提交 `55254ac`（2 files: `RecHelper.cc/.h`）。ctor 预计算轴元数据 + 单 entry last-bin cache（key 为 `(x,y)` 精确 ==）；hit 复用 bin 解析只做 4 读 + blend，miss 重算并存。judger 复核：bit-faithfulness 精确——预计算轴元数据用同表达式同字面量（200/400/0/40/0/600）故 bit-identical 常量；特化 bin-search 是逐字拷贝（同 floor/clamp/cx0/tx 算子求值序）；`inv_dx` 存但**未替** live `/dx`（躲 reciprocal 陷阱）；cache 只存 bin 解析不存 hist 状态，blend 读当前 hist GetArray；NaN/inf 在 cache 前短路返回 0.0 同未缓存。CORRECTNESS=PASS 10/10。 | score **0.20** risk **low**。"**519.9 ms/evt = +13.2% vs prior 459.5: SPEED REGRESSION (correctness PASS)**。last-bin cache 的 member-read + 不可预测分支开销 > ~25-flop bin-search 节省；假设的同 `(VVar0,tres)` 命中率被实测证伪"。实测 519.94 vs prior 459.50 **+13.2%**（回退），vs baseline −43.7%。judger 推断：单 entry cache 命中率远低于假设（若 tres 每 call 重算或跨 PMT 交错，每次 miss）；即便 hit，cache 加的 per-call 开销（读更大的 RecHelper 成员 + 不可预测 hit/miss 分支 + 依赖 load 的 cached kx/tx 喂数组索引）> 原 bin-search 节省（现代乱序核上 bin-search 本已便宜且全流水）。0.20：clean 且 bit-faithful 但 PRIMARY 目标（speed）走反，459.5 仍是更好结果。**方向建议**：(1) 剥掉 cache 只留 ctor 轴 hoist（零 per-call 开销）再测；若仍慢则轴元数据从非瓶颈，time-PDF-interp hoist 方向**已耗尽**。(2) 若耗尽则 pivot——成本不在 ~25-flop bin-search 而在数组读/GetArray 指针 chase/周边 FCN 算术（Poisson 递归、exp/log），**跑 omilrec-profile 确认 cycles 实际去哪**而非信 bin-search 假设。(3) 再 memoize 前先 instrument 实测 cache 命中率验证 `(VVar0,tres)` 假设——回退说明它错。 | **519.94** (−43.7% vs baseline) |

## 关键观察

- **R1–R3 三连涨**：924.53 → 744.06 → 671.82 → 485.18，累计 **−47.5%**。机制分别是 SoA+QPDF cache、NPE 插值预算、time-only hit-PMT 裁剪。
- **R3 risk=medium**：bit-identical 依赖隐式不变量（time-only 跳过的 `m_NPE/m_UfrmScale` 恰被下游 charge-bearing 最小化 FCN 头部重置覆盖），当前成立但脆弱。
- **R4 回退**：+6.1% vs prior，score 0.30。Part A/B 加的分支/precompute 开销 > 省下的死算术。harness 选的 **best 仍是 R3**（`b14461f`，485.18，gate-pass + risk≠high + 最低 SPEED_MS）。
- **续跑链从 R4（回退轮）接**，非从 best R3 接——SimpleLoop 是 serial 时间链，不回退到 best。continue log 确认 `resuming from round 5 (parent_sha=60ff65db47, 4 round(s) already done)`，baseline eval 重跑一次。
- **R5–R12 八轮里只有 R6(−13.1%)、R7(−2.2%)、R11(−11.0%) 三轮真涨**，R5/R9/R12 三轮回退（+5.0%/+12.1%/+13.2%），R8 空提交，R10 噪声平。**best 从 R3 的 485.18 → R7/R11 的 459.3–459.5**，八轮净推进仅 ~26 ms/evt（−2.7%），从 R7 到 R12 五轮几乎零净进展（459.32 → 519.94，反而倒退）。

### 陷进单一路径：proposer 反复在"SoA hoist per-PMT 不变量"这面墙上撞

- **SoA hoist 主题被提了 6+ 次**：R0（成功）、R6（重提 ALL-invariant SoA，小赚）、R7（又提，空提交）、R9（geometry-only 切片，回退）、R10（又 ALL-invariant SoA，噪声）、R11 的 proposal 开头也标"same mechanism as rounds 0/5/6/9 validated"。proposer 把 R0 的成功当"该杠杆永远有效"的锚，但 R1/R2/R6/R11 已把 NPE 插值、QPDF、bin-search、指针解析吃掉后，FCN 的 virtual-dispatch 早已不是主导成本。
- **enableTimeInfo-split 被硬刚 3 次**：R4（+6.1%）、R5（+5.0%，且明说"修 R4"）、R12 的 proposal 仍引用 R3/R4/R5 的失败做 grounding。每次换措辞（per-PMT guard → FCN-level split → last-bin cache）但本质同一"省死算术/分支开销"杠杆，judger 已判该方向 0/2 失败。
- **回退轮被链继承，净进度被反复吃掉**：R5 从 R4（已回退）接链 → 回退；R9 从 R8（空）接 → 回退；R12 从 R11（best）接 → 回退。best 长期停在 ~459 ms/evt 平台。

### 根本问题（loop 设计层面）

1. **proposer 不 profile，靠"R0 成功过"外推**。judger 从 R6 起每轮都说"profile FCN find the new dominant phase"，proposer 一次都没真做（Bash 都是 grep/sed 读代码，没跑 perf/SniperProfile）。一直在猜热点，猜错就回退。R12 judger 终于明说"跑 omilrec-profile 确认 cycles 实际去哪，而非信 bin-search 假设"。
2. **无放弃阈值**。一个方向 judger 判 0.25/0.20 两次失败后，proposer 下轮仍以"这是修上一轮的"为由重提。judger 的"revert to best"建议被无视（链是时间序，不回退到 best）。
3. **best 选择对回退链无保护**。R9/R12 从回退轮接链，把已回退状态当新基线继续优化，等于在错误起点上叠加。

### 当前状态

- baseline 924.53 → 当前 best **459.3 ms/evt（R7 `857972d` / R11 `9dfdbcc`，−50.3%）**。约 **2× 加速**，离 5–10× 目标还很远，且 proposer 策略空间已被"SoA hoist + 省死算术"锁死，平台已现。
